#pragma once

#include "tensor.h"

namespace ml {
namespace types {

using float_type = float;

// template <typename T = float_type> using vector = tensor<T, 1>;
// template <typename T = float_type> using maxtrix = tensor<T, 2>;

}  // namespace types
}  // namespace ml